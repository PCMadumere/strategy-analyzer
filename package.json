{
  "name": "trading-strategy-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Trading strategy research, backtesting, comparison and recommendation platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "npm run dev --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}